# UI Contracts

## Separation
UI surfaces consume runtime outputs and must not mutate kernel state directly.

## Determinism
UI interactions must not introduce nondeterministic runtime behavior.

## Status
UI contracts are placeholders pending implementation.
